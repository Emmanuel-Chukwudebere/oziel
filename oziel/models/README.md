Wake-word feature models from openWakeWord v0.5.1
(https://github.com/dscripka/openWakeWord), Apache-2.0.
melspectrogram.onnx + embedding_model.onnx turn 16 kHz audio into speech
embeddings; oziel/wake.py matches those against the owner's own takes.
